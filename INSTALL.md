# Installation

Use Python 3.10-3.12. Smoke tests are CPU-only. Full visual/RAG runs may need CUDA, OpenCV, Ultralytics, ASR/caption models, and API access.

```bash
python -m pip install -r requirements.txt
python -m pip install -r requirements-dev.txt
```
Optional baseline dependencies are listed in `requirements-optional.txt`.
